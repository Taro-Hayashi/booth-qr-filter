# AGENTS.md (booth-qr-filter 開発仕様書)

AIエージェントが本プロジェクトのコード変更や機能追加を行う際のガイドラインと仕様書です。

## プロジェクト概要
- **リポジトリ名**: `booth-qr-filter`
- **目的**: BOOTHのショップ管理画面の注文一覧において、発送コード（QRコード）発行済みの注文のみを表示する「QRコード発行済み」タブを追加する。
- **対象ブラウザ**: Chrome, Brave, Firefox, Zen, Safari (将来対応)
- **マニフェスト**: Manifest V3

## 技術仕様・設計

### 1. 名前空間 (Encapsulation)
独自のCSSクラス、ID、データ属性にはすべて `booth-qr-filter-` の接頭辞を付与します。
- `booth-qr-filter-tab-container`: 追加するタブの `li` 要素
- `booth-qr-filter-tab`: タブ内の `a` 要素
- `booth-qr-filter-hidden`: フィルタ適用時に非表示にする注文カード要素
- `booth-qr-filter-`: その他の独自クラス

### 2. セレクタ定義 (DOM Structure)
BOOTH注文管理画面 (`https://manage.booth.pm/orders`) のDOM構造に基づき、以下のセレクタを使用します。

| 対象要素 | CSSセレクタ / 判定基準 |
| :--- | :--- |
| タブコンテナ | `nav.ui-segmented-tablet-nav.centered > ul` |
| 「未発送」基準タブ | `a[href="/orders?state=paid"]` |
| 注文カード | `div.manage-list-table` （内部の `.mobile-full-basis` を使って判定） |

### 3. QRコード発行済みの判定ロジック
注文カード（`div.manage-list-table`）の内部に対し、以下を両方満たすものを「QRコード発行済み」とみなします。
1. カード内に `a.btn` または `.btn` で、テキストが **「発送完了を通知」** である要素が**存在する**。
2. カード内に `a.btn` または `.btn` で、テキストが **「発送コードを発行する」** である要素が**存在しない**。

### 4. フィルタ適用方法と他拡張機能への配慮
- 他の拡張機能（「BOOTH匿名配送パックQR一括プリント」など）が注文カードに追加したボタンやバッジの表示を崩さないため、注文カード自体をDOMから削除したり改変したりしません。
- 対象外の注文カード（`div.manage-list-table`）に対し、`display: none !important;` を適用するCSSクラス `booth-qr-filter-hidden` を付与することで非表示化します。
- **注文グループ全体の非表示**: `div.manage-list-table` 自体を非表示にすることで、その中に含まれる注文IDやスペーサー、およびテーブルに設定された枠線（`box-shadow` など）も含めて一括して完全に消去し、不要な線が残らないようにします。
- **他一括処理拡張機能との共存（スキャン除外）**: 他拡張機能（一括印刷ツールなど）が一括処理の際に対象外の注文をスキャンしてしまうのを防ぐため、非表示にする注文コンテナからは `manage-list-table` クラスを一時的に除去し、代わりに `booth-qr-filter-temp-hidden-table` クラスを付与して退避させます。これにより、他拡張機能の一括処理が現在表示されている（QRコード発行済みの）注文のみを対象にするようになります。

### 5. MutationObserver による再描画検知
BOOTHの非同期ロードや他拡張機能による描画更新に対応するため、`document.body` に対して `childList: true, subtree: true` を設定して監視します。変更検知時、タブの再挿入およびフィルタの再適用を行います。

### 6. URL・履歴（ブラウザの「戻る」ボタン）への対応
- 「QRコード発行済み」タブをクリックすると、URLの末尾にハッシュ `#qr-issued` が設定されます。
- 現在地が「未発送（`state=paid`）」以外の場合は、自動的に `/orders?state=paid#qr-issued` に遷移してフィルタを有効化します。
- `hashchange` イベントを監視しており、ブラウザの「戻る」ボタンでハッシュが消えたり変化したりした際、自動的にフィルタが解除されて元の「未発送」注文一覧（あるいは他の遷移前ページ）へ戻れる設計になっています。

## 開発・運用ルール

### 1. ドキュメントの同期更新
- プロジェクトのコード修正、機能追加、または仕様変更を行った際は、本ファイル（`AGENTS.md`）、ルートの `README.md`、および `docs/` ディレクトリ配下の markdown ファイル（`.md`）の内容に変更が生じないかを確認し、必要に応じて必ず最新の状態に更新してください。

